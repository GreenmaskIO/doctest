# doctest

Sample product documentation, consumed by the Greenmask site as a git submodule
and served at `/docs/doctest`. Pure Markdown content under `docs/` — no build tooling.
Doc versions are `major.minor` lines derived from the git tags in this repo.
